---
lane: 'done'
---

# Task: Install papaparse & Implement Import Logic

**Phase:** Backend
**Objective:** Create data import actions.

## Requirements

1.  **Install `papaparse`**.
2.  **Server Actions:** `app/actions/import.ts`.
    - `importRooms(propertyId, csvFile)`.
    - `importBookings(propertyId, csvFile)`.

## Definition of Done

- Import actions are implemented.
- (Unit tests were planned, but deferred for MVP race)
