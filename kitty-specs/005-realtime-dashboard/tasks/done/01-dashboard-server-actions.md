# Task: Dashboard Server Actions (Aggregation queries)

**Phase:** Architecture
**Objective:** Backend logic for dashboard data.

## Requirements
1.  **Server Actions:** `app/actions/dashboard.ts`.
    *   `getDashboardStats(propertyId)`: Returns aggregated counts (Occupancy, Arrivals, Departures).
    *   `getDailyActivity(propertyId)`: Returns list of specific bookings for today.

## Definition of Done
*   Unit tests pass for both actions.
